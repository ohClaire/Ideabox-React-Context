import React, { createContext } from 'react';

const AppContext = createContext();

export default AppContext;

// we will need to import this wherever we are providing or consuming context i.e. App.js
